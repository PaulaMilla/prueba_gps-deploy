apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: frontend.local  # URL estática para el frontend
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend  # Nombre del servicio
                port:
                  number: 80       # Puerto expuesto por el servicio
